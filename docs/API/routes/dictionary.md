# Route Module: `dictionary.ts`

Base path: `/api/dictionary`

## Responsibilities
- Dictionary resource access
- Search and lookup operations

## Security
- Auth requirements depend on endpoint behavior
- Input validation for query parameters
