# Route Module: `users.ts`

Base path: `/api/users` and `/api/admin/users`

## Responsibilities
- User profile and account management
- Admin-side user directory and lifecycle actions

## Security
- Self-service permissions for user-owned data
- Elevated permission requirements for admin mutations
