services:
  orchestrator:
    security_opt:
      - seccomp=unconfined
      - label=disable
    userns_mode: host
    privileged: true
    volumes:
      - .:/workspace
    working_dir: /workspace

  model_registry:
    security_opt:
      - seccomp=unconfined
      - label=disable
    userns_mode: host
